<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:RedFocus.Controls"
                    xmlns:converters="clr-namespace:RedFocus.Converters"
                    >
    <converters:CircleCornerConverter x:Key="circleCornetConverter"/>
    <ControlTemplate x:Key="switchButtonStyle" TargetType="local:SwitchButton">
        <Border x:Name="TemplateBorder" BorderThickness="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=BorderThickness}" 
                Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="{TemplateBinding Width, Converter={StaticResource circleCornetConverter}}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TemplateBorder" Property="Background" Value="Red"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>