<UserControl x:Class="RedFocus.Controls.MenuPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedFocus.Controls"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="428" d:DesignWidth="360">

    <UserControl.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Margin="5,2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource PrimaryBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{DynamicResource PrimaryBackgroundBrush}"
                BorderBrush="{DynamicResource WindowBorderBrush}"
                BorderThickness="0,1,0,0">
            <ContentControl x:Name="PageContent" Content="{Binding CurrentPage, RelativeSource={RelativeSource AncestorType=local:MenuPanel}}"/>
        </Border>

        <Grid Grid.Row="1" 
              Background="{DynamicResource PrimaryBackgroundBrush}"
              Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="TimerConfigurationButton" 
                    Grid.Column="0" 
                    Click="TimerConfigurationButton_Click"
                    Style="{StaticResource MenuButtonStyle}" Height="36">
                <Image Source="{svgc:SvgImage Source=/Icons/Timer.svg }" Width="20" Height="20">
                </Image>
            </Button>

            <Button x:Name="OptionsButton" 
                    Grid.Column="1" 
                    Click="OptionsButton_Click"
                     Height="36"
                    Style="{StaticResource MenuButtonStyle}">
                <Image Source="{svgc:SvgImage Source=/Icons/Setting.svg }"  Width="20" Height="20">
                </Image>
            </Button>

            <Button x:Name="ThemesButton" 
                    Grid.Column="2" 
                    Click="ThemesButton_Click"
                    Height="36"
                    Style="{StaticResource MenuButtonStyle}">
                <Image Source="{svgc:SvgImage Source=/Icons/Themes.svg }"  Width="20" Height="20">
                </Image>
            </Button>

            <Button x:Name="AboutButton" 
                    Grid.Column="3" 
                    Click="AboutButton_Click"
                    Height="36"
                    Style="{StaticResource MenuButtonStyle}">
                <Image Source="{svgc:SvgImage Source=/Icons/About.svg }"  Width="20" Height="20">
                </Image>
            </Button>
        </Grid>
    </Grid>
</UserControl>
