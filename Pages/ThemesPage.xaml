<UserControl x:Class="RedFocus.Pages.ThemesPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
     xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:local="clr-namespace:RedFocus.Pages"
             xmlns:controls="clr-namespace:RedFocus.Controls"
             xmlns:converters="clr-namespace:RedFocus.Converters"
             xmlns:model="clr-namespace:RedFocus.Model"
           mc:Ignorable="d" 
  d:DesignHeight="400" d:DesignWidth="340"
  UseLayoutRounding="True"
  SnapsToDevicePixels="True">
    <UserControl.Resources>
        <converters:BackgroundToForegroundConverter x:Key="BackgroundToForegroundConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="CleanListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ListBoxItem}">

                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">

                                    <ContentPresenter x:Name="ContentPresenter"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="Selector.IsSelectionActive" Value="False">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="controls:ThemeItemControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:ThemeItemControl">
                        <Grid 
                            Width="{TemplateBinding Width}" 
                            Height="{TemplateBinding Height}"
                            SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Column="0"
                                       Fill="{TemplateBinding AccentColor}"/>
                            <Border Grid.Column="1"
                                    Padding="0"
                                    Background="{TemplateBinding PrimaryColor}"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>           
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Margin="10,0"
                                               Text="{TemplateBinding Text}"
                                               Foreground="{TemplateBinding PrimaryColor, Converter={StaticResource BackgroundToForegroundConverter}}"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1"
                                               Text="&#x2713;"
                                               VerticalAlignment="Center"
                                               Foreground="{TemplateBinding AccentColor}"
                                               Visibility="{TemplateBinding IsSelected, Converter={StaticResource BooleanToVisibilityConverter}}" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="{DynamicResource PrimaryBackgroundBrush}">
        <StackPanel Margin="20">
            <TextBlock Text="Themes"
    FontSize="18"
        FontWeight="Bold"
          Foreground="{DynamicResource PrimaryTextBrush}"
    HorizontalAlignment="Center"
        Margin="0,0,0,30"/>
            <ListBox ItemsSource="{Binding Themes}" 
                 Background="Transparent"
                 BorderThickness="0"
                     Style="{StaticResource CleanListBoxStyle}"
                     >
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type model:ThemeItem}">
                        <controls:ThemeItemControl 
                            Width="325"
                            Height="50"
                            Margin="0,0,0,10"
                            PrimaryColor="{Binding PrimaryColor}"
                            Text="{Binding Name}"
                            IsSelected="{Binding IsSelected}"
                            Selected="ThemeItemControl_Selected"
                            ResourceUri="{Binding ResourceUri}"
                        />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
